// const swiperSlider = require("swiper")

// Jobs slider
const swiper = new Swiper('.swiper-container', {
  
  // Parameters
  // centeredSlides: true,
  direction: 'horizontal',
  grabCursor: true,
  slidesPerView: 'auto'

});